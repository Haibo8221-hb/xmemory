// Shared constants (safe for client-side)

// Platform fee percentage (20%)
export const PLATFORM_FEE_PERCENT = 20

// Minimum price in USD (Stripe requires at least ~.50)
export const MIN_PRICE_USD = 0.50
